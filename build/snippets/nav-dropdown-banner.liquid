{% comment %} ATTENTION!!! This is compiled version of nav-dropdown-banner.liquid and it is overriding automatically. You should use theme development tool and sources\snippets\nav-dropdown-banner.liquid file to code and save your changes. {% endcomment %}<a class="nav-banner no-hover-link {% if block.settings.white_text %}m-white{% endif %} lazyload" href="{{ block.settings.button_url }}" {{ block.shopify_attributes }}>
  {% render 'rimage', image: block.settings.background_image %}
  <div class="nav-banner__content">
    <h4 class="pretitle not-animated h2"> {{ block.settings.accent_text }}</h4>
    <h2 class="title not-animated h1"> {{ block.settings.heading }}</h2>
    <span class="btn {% if block.settings.white_text %}m-white {% else %} m-black{% endif %}" aria-hidden="true">{{ block.settings.button_text }}</span>
  </div>
</a>