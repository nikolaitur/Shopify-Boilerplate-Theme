{% comment %} ATTENTION!!! This is compiled version of rewind_menu_backup_do_not_delete.liquid and it is overriding automatically. You should use theme development tool and sources\snippets\rewind_menu_backup_do_not_delete.liquid file to code and save your changes. {% endcomment %}          <div id="rewind-menu-backup">
          [
          {% for linklist in linklists %}
          { "id" : "{{ linklist.handle }}",
            "handle" : "{{ linklist.handle }}",
            "title" : {{ linklist.title | strip_html | json  }},
            "links" : [
                {% for link in linklist.links %}
                  {"url" : "{{ link.url }}",
                 "title" : {{ link.title | strip_html | json }}}{% unless link == linklist.links.last %},{% endunless %}
                {% endfor %}
              ]
          }{% unless linklist.handle == linklists.last.handle %},{% endunless %}
          {% endfor %}
          ]
          </div>
