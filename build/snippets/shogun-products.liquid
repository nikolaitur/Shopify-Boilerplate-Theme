{% comment %} ATTENTION!!! This is compiled version of shogun-products.liquid and it is overriding automatically. You should use theme development tool and sources\snippets\shogun-products.liquid file to code and save your changes. {% endcomment %}{% comment %}
Auto-generated by Shogun.
This file can be re-written at any time.
{% endcomment %}

{% assign handles = content.metafields.shogun.products | split: "," | slice: 0, 20 %}
{% if handles.size > 0 %}
  <script type="text/javascript">
    window.__shgProducts = window.__shgProducts || {};
    {% for handle in handles %}
      {% assign currentProduct = all_products[handle] %}
      {% if currentProduct != blank %}
        window.__shgProducts["{{handle}}"] = {{currentProduct | json }};
      {% endif %}
    {% endfor %}
  </script>
{% endif %}
