{% comment %} ATTENTION!!! This is compiled version of ea-diamonds-content.liquid and it is overriding automatically. You should use theme development tool and sources\snippets\ea-diamonds-content.liquid file to code and save your changes. {% endcomment %}<style>
  #shopify-section-{{ section.id }} {
    padding: {{ set.ptop }}px 0 {{ set.pbottom }}px;
    background-color: {{ set.sectionbg }};
  }
  @media (max-width: 989px) {
    #shopify-section-{{ section.id }} {
      padding: {{ set.ptop_mob }}px 0 {{ set.pbottom_mob }}px;
    }
  }
</style>
<div class="section m-diamonds {{ class }}">
{% include 'section-bg-image' %}
  <div class="container ta-center">
    {% if set.title != blank %}
      <h3 class="title">{{ set.title }}</h3>
    {% endif %}
    {% if set.text != blank %}
      <div class="subtitle">{{ set.text }}</div>
    {% endif %}
    <div class="section__content">
      <img src="{{ 'diamond.svg' | asset_url }}" class="diamonds" alt="">
    </div> <!-- / .section__content -->
  </div>
</div>