{% comment %} ATTENTION!!! This is compiled version of ea-newsletter.liquid and it is overriding automatically. You should use theme development tool and sources\sections\ea-newsletter.liquid file to code and save your changes. {% endcomment %}{% if section.settings.visible %}
  {% render 'ea-newsletter-content', section: section %}
{% endif %}


{% schema %}
  {
  "name": "Newsletter",
  "tag": "section",
  "settings": [
    {
      "type": "checkbox",
      "id": "visible",
      "label": "Show section",
      "default": true
    },

    {
      "type": "richtext",
      "id": "title",
      "label": "Title"
    },
    {
      "type": "richtext",
      "id": "pretitle",
      "label": "Title Secondary"
    },
    {
      "type": "text",
      "id": "placeholder",
      "label": "Input default text",
      "default": "Enter your email"
    },
    {
      "type": "text",
      "id": "btn",
      "label": "Button text",
      "default": "SUBSCRIBE"
    }
  ]
}
{% endschema %}
