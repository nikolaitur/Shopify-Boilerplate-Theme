{% comment %} ATTENTION!!! This is compiled version of theme.shogun.landing.liquid and it is overriding automatically. You should use theme development tool and sources\layout\theme.shogun.landing.liquid file to code and save your changes. {% endcomment %}{% comment %}
Auto-generated by Shogun.
This file can be re-written at any time.
{% endcomment %}

{% comment %} If you want use original css and js files please set production variable false {% endcomment %}
{%- assign production = true -%}
{%- assign admin_shopify = false -%}

{%- if request.host contains 'myshopify.com' -%}
	{%- assign is_admin_interface = true -%}
	{%- assign admin_shopify = true -%}
{%- else -%}
	{%- assign is_admin_interface = false -%}
{%- endif -%}

{%- if template.name == 'cart' -%}
	{%- assign is_admin_interface = true -%}
{%- endif -%}

{%- if production == false -%}
	{%- assign is_admin_interface = true -%}
{%- endif -%}

<!doctype html>
<!--- Start of GetEmails.com Code --->
<script>
    document.addEventListener("StartOtherLoading", function(event)
{
    loadJS('https://s3-us-west-2.amazonaws.com/files.getemails.com/account/X2JH751/source/getemails.js');
    });  
</script>
<!--- End of GetEmails.com Code --->
<html lang="en">


<head>
  
  	{%- if production == true -%}
  {% include '__opt-jquery' %}	
    {% include '__opt-js-md' %}
  	{%- endif -%}
  <!-- app start ?-->

  		{%- if is_admin_interface -%}
			{{ content_for_header }}	
		{%- else -%}
			{% include '__opt-js-content-for-header' %}
			{{ js_content_for_header }}
		{%- endif -%}
    
  <!-- app end ?-->

  {% include 'head' %}
  
      
    	{%- if production == true -%}
			{% include '__opt-css-theme' %}	 
		{%- else -%}
			{% include '__original-css-theme' %}
		{%- endif -%}
  
  
  <script>var bcpo_product={{ product | json }}; {% if product.metafields.bcpo.bcpo_data %}var bcpo_data={{ product.metafields.bcpo.bcpo_data }};{% endif %} {% if shop.metafields.bcpo.bcpo_settings %}var bcpo_settings={{ shop.metafields.bcpo.bcpo_settings }};{% endif %}var bcpo_cdn = "{{'bcpo_cdn'|asset_url}}";var inventory_quantity = [];{% for v in product.variants %}inventory_quantity.push({{v.inventory_quantity}});{% endfor %}if(bcpo_product) { for (var i = 0; i < bcpo_product.variants.length; i += 1) { bcpo_product.variants[i].inventory_quantity = inventory_quantity[i]; }}</script>

  <!-- BeginConsistentCartAddon -->  
  <script>  {% if customer %}  Shopify.merge_cart_text = '{{ "consistent_cart_addon.merge_cart_text" | t }}' ;  Shopify.customer_logged_in = true ;  Shopify.customer_email = "{{ customer.email }}" ;  Shopify.log_uuids = true;  {% else %}  Shopify.customer_logged_in = false ;  Shopify.customer_email = "" ;  Shopify.log_uuids = true;  {% endif %}  </script>  
  <!-- EndConsistentCartAddon -->

  <!-- Global site tag (gtag.js) - Google Ads: 862827897 -->
  
  <script>
  document.addEventListener("StartOtherLoading", function(event)
{
    loadJS('https://www.googletagmanager.com/gtag/js?id=AW-862827897');
  
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'AW-862827897');
    });
  </script>

 	{{ shop.metafields.loox["global_html_head"] }}


  <!--[if (gt IE 9)|!(IE)]><!-->
  <script src="{{ 'lazysizes.min.js' | asset_url }}" async="async"></script>
  <!--<![endif]-->

  {% include 'shogun-head' %}

  <!-- REAMAZE INTEGRATION START - DO NOT REMOVE -->
{% include 'reamaze-config' %}
{% include 'reamaze-sso' %}
<!-- REAMAZE INTEGRATION END - DO NOT REMOVE -->


</head>

<!-- {{ template }} -->

<body class="template-{{ template.name }} {% if settings.animations_on %} animations-on {% endif %} {% if template == 'product' %}product-page--{{ product.handle }}{% endif %}" 
{% if template == 'cart' %}style="display: none;"{% endif %}
>
  
  
  {{ content_for_layout }}
  
  
  <style type="text/css">
    body, html {
      width: 100% !important;
      background: white !important;
    }
  </style>
  
</body>

</html>
