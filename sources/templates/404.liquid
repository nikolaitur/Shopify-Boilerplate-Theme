<section class="section py-5">
  <div class="container ta-center">
    <h1 class="title">{{ 'general.404.title' | t }}</h1>
    <p class="subtitle">{{ 'general.404.subtext' | t }}</p>
    <p class="subtitle">
      <a href="{{ routes.root_url }}" class="btn m-underline">{{ 'general.404.link' | t }}</a>
    </p>
  </div>
</section>
